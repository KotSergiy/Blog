<h2><%= @article.title %></h2>
<p><%= @article.text %></p>

<h4>Комментарии</h4>
<%@article.comments.each do |comment|%>
  <p>
    <%= comment.author %>
  </p>
  <p>
    <%= comment.body %>
  </p>
  <hr>
<%end%>

Ваш комментарий
<%= form_for([@article, @article.comments.build]) do |f| %>

  <p>
    Содержание
    <br>
    <%= f.text_area :body %>
  </p>

  <p>
    <%=f.submit%>
  </p>

<% end %>
